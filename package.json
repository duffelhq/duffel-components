{
  "name": "duffel-checkout",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn esbuild src/component/index.tsx --bundle --outdir=dist --minify --tree-shaking=true",
    "dev": "yarn build --watch --serve",
    "start": "yarn build --serve"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/react": "18.0.28",
    "@types/react-dom": "18.0.11",
    "esbuild": "0.17.11",
    "typescript": "4.9.5"
  }
}
